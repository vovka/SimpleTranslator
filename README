Simple translator like Cucumber's Gherkin

Save scenario to the file. Define instructions and save them into folder 'instructions'. Then run 'translate' and pass as the first command line argument path to scenario file. 
